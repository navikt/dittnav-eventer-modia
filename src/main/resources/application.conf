ktor {
  deployment {
    port = 8092
    port = ${?PORT}
  }

  application {
    modules = [no.nav.personbruker.dittnav.eventhandler.config.BootstrapKt.mainModule]
  }
}

no.nav.security.jwt {
  issuers = [
    {
      issuer_name = ${?OIDC_ISSUER}
      discoveryurl = ${?OIDC_DISCOVERY_URL}
      accepted_audience = ${?OIDC_ACCEPTED_AUDIENCE}
      cookie_name = selvbetjening-idtoken
    }
  ]
}
